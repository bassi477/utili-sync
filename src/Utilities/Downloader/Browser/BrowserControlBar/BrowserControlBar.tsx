import { Text, View } from "react-native";

function BrowserControlBar(): JSX.Element {

    return (
        <View>
            <Text>This is control bar.</Text>
        </View>
    );
}

export default BrowserControlBar;